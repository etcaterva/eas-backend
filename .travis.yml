sudo: false

before_install:
  - pip install --quiet coveralls
  - pip install codecov

language: python

python:
  - "3.6"

install:
  - pip install -r requirements/test.txt

script:
  - python manage.py makemigrations
  - coverage run --source='eas' manage.py test
  - coverage report -m --fail-under=100

  - pylint eas

after_success:
  - coveralls
