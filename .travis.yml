sudo: false

before_install:
  - pip install --quiet coveralls
  - pip install codecov

language: python

python:
  - "3.6"

install:
  - pip install -r requirements/test.txt

script:
  - coverage run --source='eas' --source='ws' manage.py test
  - coverage report -m --fail-under=100

  - pylint ws

after_success:
  - coveralls
